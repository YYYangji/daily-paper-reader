---
title: "RS-Diffuser: Risk-Sensitive Diffusion Planning with Distributional Value Guidance"
title_zh: RS-Diffuser：带有分布价值引导的风险敏感扩散规划
authors: Shiqiang Gong
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.27766v1"
tags: ["query:rpp"]
score: 9.0
evidence: 离线强化学习中的风险敏感扩散规划；基于模型的强化学习与世界模型
tldr: 离线强化学习在安全关键场景中至关重要，但现有扩散规划器通常风险中性，忽略罕见灾难性后果。提出RS-Diffuser框架，结合扩散轨迹生成与分布价值评论家，通过分位数回归估计回报分布，在去噪过程中引入基于CVaR的风险敏感梯度引导。单个模型可通过推理时风险参数灵活实现风险规避、中性或寻求行为。实验表明，该方法在风险敏感的D4RL和机器人导航任务上取得最优性能，提升整体回报和鲁棒性，减少安全违规。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27766v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 236, \"height\": 164, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27766v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1256, \"height\": 871, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27766v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1257, \"height\": 269, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27766v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1259, \"height\": 330, \"label\": \"Table\"}]"
motivation: 现有扩散规划器忽略罕见但灾难性后果，缺乏风险敏感性，难以应对安全关键离线RL场景。
method: 结合扩散轨迹生成、逆动力学模型和蒙特卡洛分布评论家，用分位数回归估计回报分布，在去噪时引入CVaR风险梯度引导。
result: 在风险敏感D4RL和机器人导航基准上，RS-Diffuser实现最佳性能，提升平均回报和最差情况鲁棒性，降低安全违规。
conclusion: RS-Diffuser通过风险敏感指导实现灵活的在线风险偏好，适用于安全关键的离线强化学习部署。
---

## 摘要
离线强化学习能够从固定数据集中学习策略，无需额外的环境交互，因此对于在线探索成本高昂或不安全的安全关键型应用具有吸引力。基于扩散的决策方法最近通过建模丰富的多模态轨迹分布，在离线强化学习中取得了强劲性能。然而，现有的扩散规划器通常风险中性，因此可能忽略罕见但灾难性的结果，而这些结果在实际部署中至关重要。在这项工作中，我们提出了RS-Diffuser，一种结合基于扩散的轨迹生成与分布价值批评家的风险敏感离线扩散规划框架。RS-Diffuser学习一个关于未来状态轨迹的扩散规划器、一个用于动作解码的独立逆动力学模型，以及一个通过分位数回归估计候选计划完整回报分布的蒙特卡洛分布批评家。在采样时，我们将风险敏感引导信号融入去噪过程，使用从尾部感知目标（如条件风险价值）计算得到的梯度，引导生成朝向期望的风险特征。因此，仅通过改变推理时的风险参数，单个训练模型就能灵活产生风险规避、风险中性或风险寻求行为。在风险敏感的D4RL和危险机器人导航基准上的大量实验表明，RS-Diffuser实现了最先进的性能，在提高整体回报和最差情况鲁棒性的同时，减少了安全违规。

## Abstract
Offline reinforcement learning enables policy learning from fixed datasets without additional environment interaction, making it appealing for safety-critical applications where online exploration is costly or unsafe. Diffusion-based decision-making methods have recently achieved strong performance in offline RL by modeling rich, multimodal trajectory distributions. However, existing diffusion planners are typically risk-neutral and therefore may overlook rare but catastrophic outcomes that are crucial in real-world deployment. In this work, we propose RS-Diffuser, a risk-sensitive offline diffusion planning framework that combines diffusion-based trajectory generation with distributional value critics. RS-Diffuser learns a diffusion planner over future state trajectories, a separate inverse dynamics model for action decoding, and a Monte Carlo distributional critic that estimates the full return distribution of candidate plans through quantile regression. At sampling time, we incorporate a risk-sensitive guidance signal into the denoising process, using gradients computed from tail-aware objectives such as Conditional Value at Risk to steer generation toward desired risk profiles. As a result, a single trained model can flexibly produce risk-averse, risk-neutral, or risk-seeking behaviors by changing only the inference-time risk parameter. Extensive experiments on risk-sensitive D4RL and risky robot navigation benchmarks demonstrate that RS-Diffuser achieves state-of-the-art performance, improving both overall return and worst-case robustness while reducing safety violations.

---

## 论文详细总结（自动生成）

# RS-Diffuser: 风险敏感扩散规划与分布价值引导——中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：离线强化学习（offline RL）在安全关键领域（如自动驾驶、机器人、医疗）极具价值，但现有扩散模型规划器（如Diffuser、Decision Diffuser）本质上是**风险中性的**，只优化期望回报，忽略罕见但灾难性的尾部分布。这限制了它们在真实高风险场景中的部署。
- **背景**：扩散模型在离线决策中取得显著成功（建模多模态轨迹分布），但缺乏对尾部风险的显式建模。风险敏感RL（如分布RL、CVaR优化）虽已提出，但多在actor-critic框架下，未与扩散规划器融合。
- **研究目标**：将**分布价值评论家**与**扩散规划器**结合，实现推理时灵活可控的风险偏好（风险规避、中性、寻求），提升最差情况性能和安全性。

## 2. 论文提出的方法论

### 核心思想
- 训练一个**状态轨迹扩散规划器**（生成未来状态序列）、一个**逆动力学模型**（将状态转换映射为动作）、一个**蒙特卡洛分布评论家**（通过分位数回归预测完整回报分布）。
- 在扩散去噪过程中，用**尾部意识风险度量（如CVaR）** 的梯度作为引导信号，偏置采样轨迹朝向期望风险特征。
- 仅需一次训练，推理时通过改变风险参数（α）即可切换行为。

### 关键技术细节
1. **规划器**：基于DDPM，预测未来H步状态序列 `x0 = (s_{t+1}, ..., s_{t+H})`，条件于当前状态st。损失函数 `L_plan = E[||ϵ - ϵ_θ(x_k, k, s_t)||^2]`。
2. **逆动力学模型**：学习解码器 `f_ψ(s_t, s_{t+1})` 预测动作，损失 `L_inv = E[||a_t - f_ψ(s_t, s_{t+1})||^2]`。
3. **分布评论家**：用N个分位数 `{z_ϕ^i(x_k, s_t, k)}` 近似回报分布。训练采用**蒙特卡洛返回**（而非Bellman自举），损失为分位数Huber损失 `L_dist = (1/N) Σ_i ρ_κ^{τ_i}(R_t - z_ϕ^i)`，其中R_t是折扣累积回报。
4. **风险引导采样**：
   - 定义风险度量：CVaR_α ≈ 平均最低α分位数；VaR_α = 第α分位数。
   - 计算梯度 `g_k = ∇_{x_k} ρ_α(x_k, s_t, k)`。
   - 更新去噪过程：`p(x_{k-1}|x_k, ...) ≈ N(μ_θ + w Σ_k g_k, Σ_k)`，w为引导强度。
5. **算法流程**：离线训练三个组件，推理时从高斯噪声开始，迭代去噪并加入风险梯度，最后用逆动力学模型解码动作。

## 3. 实验设计

### 数据集与场景
- **风险敏感D4RL**：修改MuJoCo环境（Half-Cheetah, Walker-2D, Hopper），在进入不安全状态（如速度过大、大俯仰角）时施加惩罚，产生稀疏严重负面回报。包含三个数据集难度：Medium、Expert、Mixed。
- **危险机器人导航**（Risky PointMass, Risky Ant）：起点到目标之间经过危险区域，绕路可避免惩罚。

### 对比方法
- 风险中性基线：CQL、Diffusion-QL。
- 风险敏感基线：OWCPG（最差情况策略梯度）、ORAAC（分布评论家+模仿学习）、CODAC（保守离线分布RL）、UDAC（不确定性感知actor-critic+扩散策略）。

### 评估指标
- 每个任务100个测试episode，报告**平均回报（Mean）** 和**CVaR_0.1**（最差10%的均值）。导航任务额外报告**违规次数**（进入危险区域的时间步数）。
- 结果基于5个随机种子，以`均值±标准差`报告。

## 4. 资源与算力

- **文中明确说明**：
  - 模型训练1M步，batch size=256。
  - 规划器使用2-block Transformer（隐藏维256），逆动力学MLP（隐藏维256）。
  - DDIM采样20步规划，DDPM 10步逆动力学。
  - 优化器：Adam，学习率3e-4。
- **未明确说明**：GPU型号、数量、单次实验耗时等硬件细节。因此无法精确评估算力开销。

## 5. 实验数量与充分性

- **实验组数**：
  - 主实验：三个D4RL环境×三个难度×两个评估指标，共18个结果表；两个导航任务×三个指标+违规次数，共8个结果表。总计26个指标结果。
  - 消融实验：在Half-Cheetah Medium/Expert/Mixed上，改变风险度量（CVaR_0.05, CVaR_0.1, CVaR_0.2, VaR_0.05, VaR_0.1, VaR_0.2），共6×3=18个指标结果。
- **充分性与公平性**：
  - 对比了多种主流离线RL方法（包括最新风险敏感方法），实验设置一致（均用100 episodes×5 seeds）。
  - 消融实验系统分析了不同风险度量与α的影响，验证了设计选择。
  - 不足：未在更高维真实机器人任务（如真机）上验证；未与风险敏感扩散模型的变种（如直接修改Diffuser+目标回报条件）对比；未评估计算效率或推理时间。

## 6. 论文的主要结论与发现

- RS-Diffuser在**所有任务上均取得最优或最优水平的平均回报和最差情况CVaR**，尤其在Half-Cheetah和Hopper的Medium/Expert上显著超越现有方法。
- 在导航任务中，RS-Diffuser**零违规或最低违规**同时保持最高回报，展示了安全与性能的权衡优势。
- 推理时风险参数α可灵活控制风险偏好：小α提升尾部鲁棒性但降低平均回报，大α反之。CVaR引导比VaR引导更稳健，CVaR_0.1为最佳默认选择。

## 7. 优点

1. **创新性**：首次将分布价值评论家与扩散规划器融合，实现推理时灵活风险偏好控制，无需重新训练。
2. **方法设计简洁有效**：通过蒙特卡洛返回、分位数回归、条件去噪梯度引导，避免了自举误差和分布偏移。
3. **实验全面**：覆盖多个连续控制环境和任务难度，对比多种基线，消融实验系统。
4. **性能优异**：在均值和最差情况双重指标上均领先，验证了风险敏感规划的实际价值。

## 8. 不足与局限

1. **算力与效率未透明**：未报告GPU型号、训练/推理耗时，难以评估实际部署成本。
2. **环境维度有限**：仅在中等维度连续控制任务（MuJoCo、PointMass、Ant）上验证，未涉及高维机器人（如灵巧手）或视觉输入场景。
3. **未与同类型风险敏感扩散方法对比**：如直接修改Diffuser加入CVaR目标回报条件，或使用风险敏感逆动力学，对比不够充分。
4. **风险度量依赖先验**：用户需手动设定风险参数α和风险度量类型（CVaR/VaR），缺乏自适应选择机制。
5. **潜在偏差风险**：实验仅采用5个随机种子，统计稳定性一般；未评估不同数据集质量（如噪声水平）下的鲁棒性。
6. **应用限制**：假设离线数据集已包含风险相关轨迹；若数据集本身未覆盖尾部事件，则分布评论家可能无法准确泛化。

（完）
