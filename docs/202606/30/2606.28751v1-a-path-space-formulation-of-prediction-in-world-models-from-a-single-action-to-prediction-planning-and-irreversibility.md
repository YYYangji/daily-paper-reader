---
title: "A Path-Space Formulation of Prediction in World Models: From a Single Action to Prediction, Planning, and Irreversibility"
title_zh: 世界模型中预测的路径空间形式化：从单个动作到预测、规划和不可逆性
authors: Gunn Kim
date: 2026-06-27
pdf: "https://arxiv.org/pdf/2606.28751v1"
tags: ["query:rpp"]
score: 9.0
evidence: 世界模型的预测与规划路径空间公式
tldr: 针对AI世界模型中的预测问题，本文提出路径空间公式，将未来轨迹的概率分布视为路径测度，在局部马尔可夫近似下采用Onsager-Machlup形式。通过将潜在动力学分解为可逆和不可逆成分，从模型滚动中引入熵产生的操作度量。在小型注意力模型实验中，注意力不对称性随数据不可逆性获得，对称化注意力抑制熵产生并选择性损害不可逆动力学长程预测，表明不可逆性可作为预测世界模型的资源。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28751v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 615, \"height\": 223, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28751v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1831, \"height\": 1174, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28751v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1839, \"height\": 1187, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28751v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1835, \"height\": 1548, \"label\": \"Figure\"}]"
motivation: 现有世界模型通常基于单步条件分布，缺乏对轨迹级概率度量的统一描述，尤其在不可逆动力学中预测和规划受限。
method: 提出路径空间公式，定义轨迹概率测度并采用Onsager-Machlup形式；分解动力学为可逆与不可逆成分，引入熵产生度量，并在注意力模型中验证。
result: 注意力不对称性与数据不可逆性成正比；对称化注意力降低熵产生，破坏不可逆长程预测但保留可逆弛豫预测。
conclusion: 不可逆性可作为预测世界模型的计算资源，路径空间公式统一了预测、规划和不确定性量化。
---

## 摘要
我们提出了一种AI世界模型中预测的路径空间形式化。我们认为，世界模型隐式地定义了未来轨迹上的概率测度，而非单步条件分布的序列。在潜在动力学允许有效马尔可夫描述的局部范围内，该路径测度采取Onsager-Machlup形式。在此框架下，预测（最可能轨迹）、规划（约束优化）和不确定性（涨落）作为单个作用泛函上的操作涌现。我们将潜在动力学分解为可逆和不可逆分量，并引入从模型展开中熵产生的一种可操作度量。在受控的小规模基于注意力的模型中，我们发现注意力不对称性在训练过程中与数据的不可逆性成比例地获得。对称化学习到的注意力会抑制熵产生，并选择性地退化不可逆动力学的长期预测，同时保留弛豫预测。这些结果表明，不可逆性可能作为预测性世界模型的一种计算资源。更一般地说，基本的预测对象是未来路径上的分布而非状态。

## Abstract
We propose a path-space formulation of prediction in AI world models. Rather than sequences of one-step conditional distributions, we argue that a world model implicitly defines a probability measure over future trajectories. In the local regime where latent dynamics admit an effective Markovian description, this path measure takes the Onsager-Machlup form. Within this framework, prediction (most probable trajectory), planning (constrained optimization), and uncertainty (fluctuations) emerge as operations on a single action functional. We decompose the latent dynamics into reversible and irreversible components and introduce operational measures of entropy production from model rollouts. In controlled small-scale attention-based models, we find that attention asymmetry is acquired during training in proportion to the irreversibility of the data. Symmetrizing the learned attention suppresses entropy production and selectively degrades long-horizon prediction of irreversible dynamics while preserving relaxational prediction. These results suggest that irreversibility may serve as a computational resource for predictive world models. More generally, the fundamental predictive object is a distribution over future paths rather than states.

---

## 论文详细总结（自动生成）

### 论文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **现状**：现有世界模型通常基于单步条件分布 `p(x_{t+1} | x_t)` 进行预测，预测、规划、不确定性被视为独立模块；缺乏对轨迹级概率度量的统一描述。尤其在不可逆动力学（如持续运动、节奏序列）中，单纯等待于单步预测无法捕捉长期结构。
- **核心动机**：借鉴非平衡统计物理的Onsager-Machlup形式，提出将**未来轨迹上的概率分布**视为世界模型的基本预测对象，而非状态分布。在此框架下，预测（最可能轨迹）、规划（固定端点最小作用）、不确定性（曲率）均源于同一路径作用泛函。不可逆性被视为一种可由注意力架构控制的计算资源。

#### 2. 论文提出的方法论
- **核心思想**：
  - 定义路径测度 `P[Γ]`，其中 `Γ = {z(t)}` 为潜在轨迹；在局部马尔可夫极限下，路径测度取Onsager-Machlup形式：`P[Γ] ∝ e^{-A[Γ]}`，作用泛函 `A[Γ] = ∫ dt [ (1/(4D)) |ż - f(z)|² + (1/2) ∇·f ]`。
  - 将漂移 `f` 分解为可逆梯度部分（`-∇U`）与不可逆循环部分（`v`），满足 `∇·(ρ_ss v)=0`。根据是否满足细致平衡（`v≡0`）判断可逆性。
  - 路径熵产生：`Σ[Γ] = ln(P[Γ]/P[Γ̃]) = (1/D) ∫ v·dz`；稳态熵产生率 `˙Σ = (1/D) ⟨|v|²⟩_ss`。
- **关键技术细节**：
  - **预测**：最可能轨迹由变分 `δA=0` 导出牛顿方程 `ż̈ = 2J_A ż - ∇V_eff`，其中 `J_A = (J - Jᵀ)/2` 为漂移雅可比反对称部分，起洛伦兹力作用；该轨迹一般不等于确定性滚动 `ż = f`。
  - **注意力与局部作用对应**：单层注意力读出 `f(z) = Attn(z) - z`，其中注意力分数 `s_s(z) = (1/√d) zᵀ M m_s`，`M = W_Qᵀ W_K`。推导出漂移雅可比 `J = (1/√d) Cov_a(v, M m) - I`；在值绑定（`v_s = m_s`）下，`J_A = (1/(2√d)) ( [C, M_S] - {C, M_A} )`，其中 `C = Cov_a(m,m)`，`M_A` 为 `M` 的反对称部分。因此注意力不对称性 `Q = ||M_A||_F / ||M||_F` 是循环漂移和熵产生的可控源头。
- **算法/流程**：
  - 从滚动轨迹估计漂移 `f̂` 和扩散 `D̂`（Kramers–Moyal系数）以及平稳密度 `ρ_ss`，计算概率流 `J = f̂ρ_ss - D̂∇ρ_ss` 与循环漂移 `v = J/ρ_ss`，进而得到 `˙Σ`。
  - 干预方法：将 `M` 对称化为 `(M + Mᵀ)/2`，使 `Q→0`，观察 `˙Σ` 下降。

#### 3. 实验设计
- **使用数据/场景**：受控的二维潜在动力学模型，数据由 Ornstein–Uhlenbeck 加旋转生成：`dz = (-a z + ω R z) dt + √(2D) dW`（`R` 为旋转生成器），通过调节 `ω` 控制不可逆性（真熵产生率 `˙Σ_true = 2ω²/a`）。使用线性注意力和 softmax 注意力两种架构（潜在空间 2D）。
- **Benchmark**：线性模型下精确熵产生率（Lyapunov 方程解析解）作为真值。
- **对比方法**：主要对比训练后模型与对称化（`Q→0`）后的模型；未与其他世界模型方法（如 RSSM、Dreamer）直接对比。

#### 4. 资源与算力
- 论文未明确提及所用 GPU 型号、数量及训练时长。实验基于小规模二维玩具模型，训练步骤仅为数百步，算力需求极低（单块 CPU/GPU 即可完成），与常规大规模世界模型（如 4×A100 训练数天）不可同日而语。

#### 5. 实验数量与充分性
- 实验覆盖：
  - (a) 线性模型：扫掠 `θ(0→0.97)`，测量 `˙Σ` 并与精确值对比。
  - (b) softmax 模型：同样扫掠 `Q`，测量 `˙Σ`。
  - (c) 干预实验：对称化后比较 `˙Σ` 和概率流矢量场。
  - (d) 训练实验：从对称初始化训练，观察 `||M_A||`、`˙Σ`、非正态性 `N` 随训练步骤及数据不可逆性 `ω` 的变化。
  - (e) 预测损失对比：对称化后循环和弛豫数据上的单步损失及多步滚动误差。
- 充分性评价：
  - **充分**：验证了所提的五项可证伪预测中的四项（P2–P5），机械论路径完整（从架构量→热力学量→功能影响）。干预实验提供了因果证据。
  - **不足**：未验证局部性假设（P1，即记忆时间 `τ_mem` 远小于动力学时间 `τ_dyn` 的条件），仅在二维低维空间进行，缺乏高维（>10）潜在空间验证；未使用真实世界模型评测任务（如 Atari、Minecraft），基准单一。未对比其他熵产生估计方法。

#### 6. 论文的主要结论与发现
- 注意力不对称性（`Q`）是循环漂移和熵产生的主要架构来源；训练过程中模型自动习得与数据不可逆性成比例的 `Q` 和 `˙Σ`。
- 对称化注意力（`Q→0`）使 `˙Σ` 降低一个数量级以上，并选择性破坏长期循环轨迹预测（误差增长），但保留弛豫（梯度）预测性能。
- 不可逆性（熵产生）是预测长期持续动态结构的“计算资源”，而非仅仅是学习的副产品。
- 基本预测对象是未来轨迹分布，而非状态分布；此路径空间公式统一了预测、规划与不确定性。

#### 7. 优点
- **理论创新**：将非平衡统计物理（Onsager–Machlup、熵产生、奇数弹性）与注意力架构有机结合，提供统一的解释框架，使热力学量（`˙Σ`）成为可计算、可干预的工程量。
- **可证伪性与因果验证**：明确提出五项可证伪预测，并通过对称化干预实验建立从架构到热力学再到功能的因果链，而非仅相关。
- **简洁的实验验证**：使用二维玩具模型清晰展示机理，透明可复现；每个实验均对照精确解或噪声地板，测量可靠。
- **洞察启发性**：表明世界模型的不可逆性可能是一种可控资源，为未来设计有向、长程预测模型提供新思路。

#### 8. 不足与局限
- **维度与规模限制**：潜在空间仅为2维，未在高维（如 ≥64 维的 RSSM 潜在空间）验证。网格法估计 `ρ_ss` 和 `f` 在高维不可行，论文也未给出可用高维估计算法的具体实施细节。
- 未评估实际大规模预训练模型（如 DreamerV3、Transformer 世界模型）中的效应；因此无法断言注意力不对称性在大模型中仍是主导不可逆性来源。
- **局部性假设未验证**：记忆时间 `τ_mem` 与动力学时间 `τ_dyn` 之比 `ϵ` 在深度模型中可能不小，导致 Onsager–Machlup 形式需非局部修正；论文未测量或约束 `ϵ`。
- **模型简化**：实验仅使用值绑定（`v_s = m_s`）的注意力；实际注意力中值路径（`v_s` 独立可学习）可能携带不可逆性，且未被分析。残差连接、前馈层、层归一化的影响未被隔离。
- **残余偏差**：对称化后 `˙Σ ≈ 0.2` 的噪声地板来自有限箱格偏差，限制了低 `Q` 区间的动态范围；未通过多分辨率分析分离系统性与统计误差。
- 应用范围狭窄：未在规划（固定端点）任务或高维控制问题中评测；未来需要大规模实验验证。

（完）
