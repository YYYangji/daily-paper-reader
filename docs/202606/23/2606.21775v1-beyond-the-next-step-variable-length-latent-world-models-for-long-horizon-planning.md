---
title: "Beyond the Next Step: Variable-Length Latent World Models for Long-Horizon Planning"
title_zh: 超越下一步：用于长时程规划的变长隐世界模型
authors: "Tianqi Du, Qi Zhang, Yifei Wang, Yisen Wang"
date: 2026-06-19
pdf: "https://arxiv.org/pdf/2606.21775v1"
tags: ["query:rpp"]
score: 9.0
evidence: 用于长期规划的变长潜在世界模型
tldr: "现有隐空间世界模型通过单步预测递归实现长程规划，导致累积误差。本文提出VLWM，直接学习变长动作序列的隐状态预测，结合课程训练逐步扩展预测长度。在长程控制任务上平均提升13%，尤其长程规划场景。VLWM为改进隐空间世界模型的长程预测与规划提供了简单有效的范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21775v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1350, \"height\": 807, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21775v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 355, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21775v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1329, \"height\": 678, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21775v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1161, \"height\": 225, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21775v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1198, \"height\": 366, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21775v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1019, \"height\": 224, \"label\": \"Table\"}]"
motivation: 现有隐空间世界模型依赖单步预测递归用于长程规划，导致累积误差与训练-规划目标不匹配。
method: 提出VLWM，学习预测变长动作序列的隐状态，并采用课程训练逐步扩展预测范围，规划时利用变长预测能力。
result: "在长程控制任务上平均超越SOTA方法LeWM 13%，长程规划任务提升显著。"
conclusion: VLWM为提升隐空间世界模型的长程预测与规划提供了简单有效的范式。
---

## 摘要
近来，世界模型通过学习基于观测和动作预测未来环境状态的预测模型，已成为构建智能智能体的有前景范式。特别是，JEPA风格的隐世界模型通过在紧凑表示空间中学习动作条件动力学，提供了像素空间预测的高效替代方案。然而，现有隐世界模型通常依赖单步预测，并需递归展开以实现长时程规划，这导致误差累积以及训练目标与下游规划任务之间的不匹配。为解决此限制，我们提出变长隐世界模型（VLWMs），该框架学习根据可变长度动作序列预测未来隐状态。VLWMs并非仅训练单步转移，而是直接建模时间扩展动力学，使同一预测器能评估不同规划水平上的动作计划。我们进一步引入课程训练策略，逐步扩展动作规划水平，从短程动力学到长程预测稳定优化。在测试时，我们设计专为VLWMs定制的规划方法以更好地利用其变长预测能力。在长时程控制任务上的实验表明，VLWMs显著改进了隐空间世界模型，在不同数据集上相比最先进的LeWM平均提升13%，尤其在需要扩展规划的任务上增益显著。这些结果表明，VLWM为改进隐世界模型中的长时程预测与规划提供了一种简单而有效的范式。

## Abstract
Recently, world models have emerged as a promising paradigm for building intelligent agents by learning predictive models that estimate future environment states conditioned on observations and actions. In particular, JEPA-style latent world models provide an efficient alternative to pixel space prediction by learning action-conditioned dynamics in compact representation spaces. However, existing latent world models typically rely on one-step prediction and must be recursively rolled out for long-horizon planning, which leads to compounding errors and a mismatch between training objectives and downstream planning tasks. To address this limitation, we propose Variable-length Latent World Models (VLWMs), a framework that learns to predict future latent states conditioned on action sequences of variable lengths. Instead of training only on one-step transitions, VLWMs directly model temporally extended dynamics, allowing the same predictor to evaluate action plans over different horizons. We further introduce a curriculum training strategy that progressively expands the action horizon, stabilizing optimization from short-range dynamics to long-range prediction. At test time, we design planning methods tailored to VLWMs to better exploit their variable-length predictive capabilities. Experiments on long-horizon control tasks show that VLWMs significantly improve latent space world models, achieving 13\% average improvement over the state-of-the-art LeWM across different datasets, with especially large gains on tasks requiring extended planning. These results suggest that VLWM provides a simple yet effective paradigm for improving long-horizon prediction and planning in latent world models.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机与背景）

现有隐空间世界模型（如 JEPA、DINO-WM、LeWM）通常采用**固定单步预测**的训练范式，即在隐空间中仅监督一步转移（t → t+1）。在长时程规划任务中，模型必须递归展开多步预测，这导致两个关键瓶颈：

- **累积误差**：单步预测的误差在递归展开中逐步放大，导致轨迹漂移。
- **训练-规划不匹配**：训练目标仅捕捉短时局部动力学，与下游长程规划所需的全局一致性不一致；且相邻帧高度冗余，模型容易学会“近似恒等映射”，忽略了高层语义结构。

为此，论文提出 **变长隐世界模型（VLWM）**，旨在通过直接学习**变长动作序列到未来隐状态的映射**，统一短程与长程动力学学习，从根本上缓解递归误差累积问题，使同一预测器能在不同规划水平上评估动作计划。

## 2. 方法论：核心思想、关键技术细节

### 2.1 核心思想
将固定单步预测泛化为**变长跳步预测**：给定当前隐状态 \(s_t\) 和一段长度为 \(k\) 的动作序列 \(a_{t:t+k-1}\)，直接预测 \(k\) 步后的隐状态 \(\hat{s}_{t+k}\)，其中 \(k\) 在训练时可由分布 \(p(k)\) 采样（支持 \(1 \le k \le K_{\text{max}}\)）。

### 2.2 关键技术细节

- **变长监督目标**：  
  \[
  \mathcal{L}_{\text{VLWM}} = \mathbb{E}_{t, k \sim p(k)} \left\| g_\phi(s_t, a_{t:t+k-1}) - \text{sg}\!\left(\bar{f}_{\bar{\theta}}(o_{t+k})\right) \right\|_2^2
  \]
  其中 \(s_t = f_\theta(o_t)\) 为在线编码器输出，\(\bar{f}_{\bar{\theta}}\) 为目标编码器（EMA），\(g_\phi\) 为预测器。固定单步预测（\(k=1\)）是特例。

- **动作-即-令牌（Action-as-Token）架构**：  
  将每个动作嵌入 \(e_\tau = h_\psi(a_\tau)\) 视为标准 Transformer 令牌，与状态令牌交错形成输入序列：
  \[
  X = [s_{t-L+1}, \dots, s_{t-1}, s_t, e_t, e_{t+1}, \dots, e_{t+k-1}]
  \]
  使用因果注意力，直接从最后一个输出令牌读出预测隐状态 \(\hat{s}_{t+k}\)。无需特殊掩码或预测令牌。状态与动作令牌通过加性类型嵌入区分，位置嵌入共享。

- **课程训练策略（Curriculum Training）**：  
  将训练分为 \(K_{\text{max}}\) 个阶段，每个阶段逐步扩大允许的预测范围。第 \(j\) 阶段从 \(\{1,\dots,j\}\) 中均匀采样 \(k\)。这种方法稳定了长程预测的优化，避免直接训练大跨度导致梯度噪声和表示崩溃。

- **规划方法**：  
  在模型预测控制（MPC）框架中，将规划步长 \(H\) 分割为若干变长子块：
  \[
  s_t \xrightarrow{k_1} \hat{s}_{t+k_1} \xrightarrow{k_2} \cdots \xrightarrow{k_M} \hat{s}_{t+H}
  \]
  作者设计了三种规划策略：
  - **P1（固定自适应）**：使用恒定块大小 \(k^*\)（根据任务选取）。
  - **P2（随机采样）**：均匀随机采样块大小。
  - **P3（长到短）**：早期用大块快速覆盖长程，后期用小块精细化。

所有规划器共享同一 VLWM 权重，仅改变块调度。

## 3. 实验设计

### 3.1 数据集 / 场景
- **PushT**：2D 连续推块任务（20,000 条专家轨迹，平均长度 196 步）。
- **OGBench-Cube**：3D 机器人单立方体拾放任务（10,000 条轨迹，每段 200 步）。
- **TwoRoom**：2D 两房间导航任务，需通过狭窄门（10,000 条轨迹，平均长度 92 步）。

### 3.2 评估协议
- **目标条件规划**：给定初始观测 \(o_0\)，目标为 \(\Delta\) 步后的观测，\(\Delta \in \{25, 50, 75, 100\}\)。
- 求解器：交叉熵方法（CEM），隐空间执行模型预测控制（MPC），每步重规划。
- 评价指标：成功率（Success Rate，%）。

### 3.3 对比方法
- 基线：**LeWM**（最先进的隐世界模型，固定单步预测滚动展开）。
- VLWM 变体：P1（固定 \(k^*=3,5\)）、P2（随机）、P3（长到短，\(K_{\text{max}}=3,5\)）。

## 4. 资源与算力

论文**未明确说明**所使用的 GPU 型号、数量、训练时长等具体硬件资源。仅在附录中提到使用 bfloat16 混合精度训练、batch size 128、AdamW 优化器，但未提及训练时间或硬件配置。

## 5. 实验数量与充分性

- **主实验**：三个环境 × 四个目标偏移 × VLWM（Oracle 规划器） vs. LeWM → 共 12 个对比点，所有偏移下 VLWM 均优于 LeWM。
- **规划策略消融**：表1 完整展示了 P1、P2、P3 及不同超参数下的成功率，共 4 种规划策略 × 4 个偏移 × 3 个环境 = 48 个数据点。
- **课程训练消融**：表2 比较有无课程训练的效果，共 24 个数据点（2 种训练方式 × 4 个偏移 × 3 个环境）。
- **表示质量分析**：表3 对 PushT 帧的 3 个物理属性（智能体位置、块位置、块角度）进行线性探测，报告 MSE 和 Pearson 相关系数。
- **定性可视化**：图3 展示 TwoRoom 上的轨迹对比。

**评估**：实验涵盖短程到长程多类任务，消融充分（规划策略、课程学习、表示质量），对比基线唯一且公平（与 LeWM 共享底层骨干、CEM 超参数、训练数据）。唯一的不足是缺少与其他变长世界模型（如 HWM）的直接对比，但论文在相关工作中已讨论差异。

## 6. 主要结论与发现

1. **VLWM 一致优于固定单步世界模型**：在三个数据集上，VLWM（Oracle 规划器）在所有目标偏移下都优于 LeWM，在长程（Δ=75,100）上提升尤其显著（如 TwoRoom-100 从 36% → 68%）。
2. **课程训练对长程任务至关重要**：去除课程训练后，PushT 和 TwoRoom 的长程成功率下降；OGBench-Cube 受益较小但仍无损。
3. **规划策略无绝对最优解**：不同环境和偏移下，随机规划 P2 或长到短 P3 各有优势，提示需要自适应的块大小选择。
4. **变长监督改善隐表示质量**：线性探测显示 VLWM 的编码器学习到更可预测的物理属性（块角度提升最大），表明长程监督鼓励编码器捕获语义结构而非局部外观。

## 7. 优点

- **方法简洁有效**：通过变长监督自然统一短程与长程预测，无需额外模块或分层结构。
- **架构灵活**：动作-即-令牌的序列化设计兼容任何编码器，且现有固定步模型可视为特例。
- **规划接口丰富**：同一模型支持多种块调度策略，为层次化规划提供新思路。
- **实验系统且公平**：与最强基线 LeWM 共享所有可重用组件，严格控制变量；消融实验覆盖规划策略、课程学习、表示质量。
- **性能提升显著**：平均提升 13%，长程任务提升幅度更大。

## 8. 不足与局限

- **未报告资源消耗**：缺少 GPU 型号、训练时间、显存占用等算力信息，难以评估方法的效率与可复现性。
- **缺少与其他变长世界模型的直接对比**：论文仅与 LeWM 比较，未与同样探索不同动作长度的 HWM 等近期工作在同一设置下对比。
- **规划策略自动化缺失**：目前需要手工选择或随机采样 chunk 大小，未提出根据任务动态自适应选择的方法。
- **OGBench-Cube 上增益较小**：课程训练在该环境几乎无益，说明方法对某些任务类型（其中长程并不比短程更难）可能增益有限。
- **仅评估离线规划**：未在在线强化学习或真实机器人环境中评估，应用至真实动态任务可能面临分布偏移。
- **最大预测跨度有限**：实验中 \(K_{\text{max}}=5\)（实际环境步长 25），更长规划（如 200 步）的效果未探索。

（完）
