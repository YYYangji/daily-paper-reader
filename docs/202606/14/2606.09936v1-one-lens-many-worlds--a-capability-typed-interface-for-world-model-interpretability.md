---
title: "One Lens, Many Worlds : A Capability-Typed Interface for World-Model Interpretability"
title_zh: 一个镜头，众多世界：面向世界模型可解释性的能力类型化接口
authors: "Bhavith Chandra Challagundla, Sanskar Pandey, Param Thakkar, Rishikesh Mallagundla, Yugandhar Reddy Gogireddy, Wenhao Lu, Hindol Roy Choudhury, Shravani Challagundla, Mohamed Deraz Nasr, Spursh Deshpande"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.09936v1"
tags: ["query:rpp"]
score: 9.0
evidence: 针对PlaNet和Dreamer等潜在循环世界模型的可解释性接口
tldr: 世界模型架构多样，现有可解释性工具因假设语言模型而重复实现。本文提出WorldModelLens，通过能力类型适配器统一接口：模型需实现编码、转移、初始状态、采样四方法，并声明可选头部。单个钩子层暴露时间索引激活、想象回放等，使分析一次编写即可复用，降低了可解释性研究的工程成本。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09936v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 977, \"height\": 660, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09936v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1119, \"height\": 474, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09936v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 859, \"height\": 387, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09936v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 789, \"height\": 419, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.09936v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1466, \"height\": 493, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09936v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 937, \"height\": 235, \"label\": \"Table\"}]"
motivation: 不同世界模型的可解释性原语相同，但工具碎片化导致重复开发。
method: 设计能力类型接口，模型实现四个必需方法和可选头部，通过钩子层暴露激活。
result: 单个钩子和缓存层支持时间索引激活、想象回放等，分析代码可跨架构复用。
conclusion: 统一接口降低了世界模型可解释性研究的工程成本，促进跨架构比较。
---

## 摘要
世界模型现在构建在本质上不同的计算基座上。基于潜状态空间模型的PlaNet和Dreamer家族将观测压缩为循环状态；基于令牌的模型如IRIS将观测量化为学习到的码本，并使用transformer进行自回归预测；联合嵌入预测架构如I-JEPA在没有像素解码器的情况下在学习的潜空间中进行预测。应用于这些模型的可解释性方法，包括探测、激活修补、稀疏自编码器和惊喜分析，共享一组共同的基元，但每种架构都从头重新实现，因为现有的钩子和缓存工具假设是一个没有动作、环境步骤或想象展开概念的transformer语言模型。我们认为这种碎片化反映了工具而非模型，并且世界模型的共享结构由一个小类型接口捕获。我们提出了WorldModelLens，一个围绕能力类型化适配器组织的开源可解释性基板：每个模型实现四个必需方法（编码、转移、初始状态、采样），并通过显式的能力描述符声明一组可选头（解码、奖励、继续、行动者、评论家），使得强化学习和自监督世界模型都是一等公民，而无需模仿对方。单一的钩子和缓存层在此接口上暴露时间索引的激活、想象展开和干预回放，允许每个分析只编写一次。

## Abstract
World models are now built on substantially different computational substrates. Latent recurrent state-space models such as PlaNet and the Dreamer family compress observations into recurrent states; token-based models such as IRIS quantize observations into a learned codebook and predict autoregressively with a transformer; and joint-embedding predictive architectures such as I-JEPA predict in a learned latent space with no pixel decoder. The interpretability methods applied to these models, including probing, activation patching, sparse autoencoders, and surprise analysis, share a common set of primitives, yet they are re-implemented from scratch for each architecture because existing hook-and-cache tooling assumes a transformer language model with no notion of actions, environment steps, or imagined rollouts. We argue that this fragmentation reflects the tooling rather than the models, and that the shared structure of world models is captured by a small typed interface. We present WorldModelLens, an open-source interpretability substrate organized around a capability-typed adapter: every model implements four required methods (encode, transition, initial state, sample) and declares a set of optional heads (decode, reward, continue, actor, critic) through an explicit capability descriptor, so that reinforcement-learning and self-supervised world models are first-class without either imitating the other. A single hook and cache layer exposes time-indexed activations, imagination rollouts, and intervention replay over this interface, allowing each analysis to be written once.

---

## 论文详细总结（自动生成）

## 论文中文总结

### 1. 核心问题与整体含义（研究动机与背景）

- **研究动机**：世界模型（world models）架构日益多样化，包括基于潜状态空间模型的 Dreamer/PlaNet、基于令牌的 IRIS/Decision Transformer、以及联合嵌入预测架构 I-JEPA 等。然而，现有可解释性工具（如 TransformerLens 等）假设模型是语言模型，无法处理动作输入、环境步、想象回放等世界模型特有的语义。这导致针对不同世界模型的可解释性分析需要重复实现，结果难以跨架构比较。
- **整体含义**：作者认为这种碎片化源于工具而非模型本身。世界模型共享一个小的类型化接口，可以统一描述所有架构。提出 WorldModelLens，通过能力类型适配器（capability-typed adapter）和钩子-缓存层，让一次编写的分析代码适用于所有世界模型，降低可解释性研究的工程成本，促进跨架构发现的可迁移性。

### 2. 方法论：核心思想、技术细节

- **核心思想**：定义一套统一的能力类型化接口，将世界模型分为：
  - **必需核心**：四个方法 `encode`, `transition`, `initial_state`, `sample_z`。
  - **可选头**：五个可选方法 `decode`, `predict_reward`, `predict_continue`, `actor_forward`, `critic_forward`，通过能力描述符 `WorldModelCapabilities` 声明（共7个布尔字段：has_decoder, has_reward_head, has_continue_head, has_actor, has_critic, uses_actions, is_rl_trained）。
- **三层架构**：
  - **适配层**：`BaseModelAdapter` 子类化，将具体模型映射到统一接口。
  - **钩子与缓存层**：`HookedWorldModel` 包装适配器，挂载命名钩子点，通过 `HookCacheManager` 管理时间索引的激活存储。
  - **分析层**：所有分析（探测、激活修补、稀疏自编码器、惊奇分析等）通过钩子和缓存接口编写，不涉及具体架构。
- **关键技术细节**：
  - 钩子点按 `(component_name, time_index)` 标识，支持 `pre/post` 阶段、单步或范围指定。
  - 支持两种入口：`run_with_cache`（记录激活到 `ActivationCache`）和 `run_with_hooks`（安装临时函数进行编辑）。
  - 实现想象展开（`imagine`）和干预重放（`intervention replay`）以支持反事实推理。
  - 每步惊奇值通过 KL 散度计算：`surprise_t = D_KL(qθ(z|ot,s_{t-1}) || pθ(z|st))`。
- **公式/算法**：论文中给出了稀疏自编码器目标函数、积分梯度、中心核对齐等标准定义，激活修补算法如 Algorithm 1 所示（clean run → cache → corrupt run with patch hook → measure effect）。

### 3. 实验设计

- **数据集/场景**：主要针对 I-JEPA 模型（Vision Transformer 作为预测器）进行案例研究；适配器覆盖了 DreamerV1/V2/V3、PlaNet、TD-MPC2、IRIS、Decision Transformer、I-JEPA、Ha-Schmidhuber 等家族。
- **Benchmark**：无标准 benchmark；实验评估三个方面：
  1. 接口覆盖能力（能力类型化是否涵盖不同家族）。
  2. 在真实模型上恢复有意义的结构——使用 I-JEPA 预测器进行层内对比：通过积分梯度（IG）和注意力权重两种方式计算上下文补丁重要性，比较 Jaccard 重叠与 Spearman 相关系数。
  3. 钩子层开销：在 I-JEPA 适配器上测量每步延迟（裸适配器 vs 挂载钩子 vs 完整缓存）。
- **对比方法**：主要是注意力权重与积分梯度这两种归因方法的对比。

### 4. 资源与算力

- 论文未明确说明具体 GPU 型号、数量、训练时长。仅提到“硬件和平均细节待定”，开销测量结果（Table 2）中的数值是初步的，需最终确定。因此无法获取具体算力信息。

### 5. 实验数量与充分性

- **实验数量**：主要三组实验：
  1. 适配器覆盖（Table 1）——列出 8 个家族的实现和声明的能力。
  2. I-JEPA 案例研究——单一模型，4 层预测器，计算每层 Spearman 相关（Figure 4），以及 Jaccard 重叠（文中文字描述）。
  3. 开销测量（Table 2）——三种条件下的延时。
- **充分性**：案例研究仅针对一个模型系列（I-JEPA），且实验只做了归因一致性分析。论文承认 “深度的实证展示仅在一个家族上”，跨架构的完整对比研究正在进行（Dreamer 全规模、V-JEPA、Cosmos）。消融实验、不同数据集验证等缺失。因此实验覆盖不完全充分，但作为方法论介绍，初步验证了接口可行性和效率。公平性方面，评估指标清晰（Spearman、Jaccard、延迟），但缺乏与其他工具（如 NNsight、Captum）的对比。

### 6. 主要结论与发现

- **接口通用性**：通过能力类型化适配器，8 个世界模型家族可以统一到同一接口下，分析代码无须修改。
- **案例研究发现**：在 I-JEPA 预测器中，注意力权重与积分梯度给出的补丁重要性排名在多数层相关性很低或为负，说明高注意力补丁不一定是因果重要的补丁，与“注意力不是解释”的已有警告一致。
- **开销可接受**：在不活跃状态下，钩子层增加约 12% 每步延迟，可用于训练/控制回路中的持续遥测；完整缓存开销较大（约 6.7 倍），适合离线分析。

### 7. 优点

- **方法设计亮点**：
  - 能力类型化接口优雅地统一了强化学习和自监督模型，无需伪造缺失的头。
  - 钩子-缓存层设计支持世界模型特有的时间索引、想象展开、干预重放，比现有语言模型工具更适合世界模型可解释性。
  - 三层分离（适配器/钩子/分析）降低了扩展新模型的成本。
  - 支持多种分析（探测、修补、SAE、惊奇分析）一次性实现，可跨架构复用。
- **实验亮点**：
  - 在 I-JEPA 上展示了分析流程的端到端工作，并复现了已知发现（注意力权重不可靠）。
  - 测量了实际性能开销，证明非活跃钩子可接受。

### 8. 不足与局限

- **实验覆盖不足**：
  - 深入的案例分析仅针对 I-JEPA，缺乏在 Dreamer、IRIS 等模型上进行相同分析（如激活修补、SAE 训练）的跨架构对比。
  - 未在多种任务/数据集上验证，如 Atari、DMControl、机器人仿真等。
- **工程限制**：
  - 一些适配器是“忠实重新实现”而非加载原始发布权重（文档中已标记）。
  - 性能测量未给出完整硬件细节和方差。
- **潜在偏差风险**：
  - 案例研究中仅使用积分梯度与注意力权重对比，未采用其他归因方法（如 LIME、Grad-CAM），结论可能受限于方法选择。
  - 未评估不同世界模型在相同分析下的行为差异，因此“统一接口带来可迁移发现”的论点赞有前提。
- **应用限制**：
  - 钩子层非活跃时开销虽小（12%），但完整缓存开销很大（6.7×），不适合在线训练。
  - 需要用户自定义适配器，对新模型集成有一定门槛。
- **声明有限**：论文提到是 alpha 软件，部分测量待定，跨架构实证在未来工作中完成。

（完）
