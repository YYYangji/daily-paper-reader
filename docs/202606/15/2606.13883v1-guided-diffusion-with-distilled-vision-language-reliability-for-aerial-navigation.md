---
title: Guided Diffusion with Distilled Vision-Language Reliability for Aerial Navigation
title_zh: 利用蒸馏视觉语言可靠性的引导扩散用于空中导航
authors: "Ivan Valuev, Iana Zhura, Valerii Serpiva, Didar Seyidov, Dzmitry Tsetserukou"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.13883v1"
tags: ["query:rpp"]
score: 9.0
evidence: 面向无人机导航的可靠性感知扩散规划器
tldr: "端到端生成模型用于无人机导航时，因无法识别玻璃、镜面等不可靠区域而失效。本文提出可靠性感知扩散规划器，通过蒸馏VLM生成场景级可靠性热图，并设计可微两阶段ESDF成本同时处理深度障碍与虚拟障碍。在仿真和实物飞行中，障碍违例率从40.3%降至9.6%，平均可靠性从0.588升至0.925，蒸馏使推理加速2倍。该方法无需重训练即可泛化至未知环境。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.13883v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1230, \"height\": 754, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13883v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 842, \"height\": 543, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.13883v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 455, \"height\": 261, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13883v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1372, \"height\": 513, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13883v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1437, \"height\": 470, \"label\": \"Table\"}]"
motivation: 端到端无人机导航模型依赖纯净数据训练，无法识别玻璃、镜面等不可靠观测区域，导致规划失败。
method: 提出可靠性感知扩散规划器，蒸馏VLM实时生成场景级可靠性热图，并利用可微两阶段ESDF成本联合处理深度与虚拟障碍。
result: "仿真和真实飞行中，障碍违例率从40.3%降至9.6%，平均可靠性从0.588升至0.925，蒸馏加速2倍。"
conclusion: 可靠性热图是决策关键，该方法无需重训练即可泛化至未知环境，显著提升规划安全性。
---

## 摘要
自主无人机导航通常通过将感知、建图和规划分离为不同阶段的流水线来解决，这会导致误差传播、累积延迟并需要针对特定环境重新调整。端到端生成模型通过将原始观测直接映射到轨迹来消除这些接口，但继承了一个微妙的故障模式：在干净数据上训练后，它们无法识别观测不可靠的情况，并将退化区域（如玻璃、镜子、过曝表面）视为有效的规划证据。我们提出了一种面向3D无人机导航的可靠性感知扩散规划器。它根据观测以及场景级可靠性热力图（标记感知不可信的区域）来条件化轨迹生成，该热力图由一个轻量级网络生成，该网络在实时规划预算内蒸馏了视觉语言模型的开放词汇推理能力。为了泛化到未见环境而无需重新训练，我们使用可微的两阶段ESDF成本来引导去噪过程，该成本同等对待来自深度的物理障碍和来自高度不可靠区域的虚拟障碍。在仿真和真实四旋翼无人机上，我们的规划器比最先进的扩散基线产生了明显更安全的轨迹，将障碍违反率从40.3%降低到9.6%，并将穿越区域的平均可靠性从0.588提高到0.925。仅消融可靠性项就会使平均可靠性从0.898降至0.783，确认了它是决定性组件，而蒸馏运行框架的速度比完整视觉语言模型快2倍。

## Abstract
Autonomous UAV navigation is conventionally solved by pipelines that separate perception, mapping, and planning into distinct stages, which propagates errors, accumulates latency, and requires environment-specific retuning. End-to-end generative models remove these interfaces by mapping raw observations directly to trajectories, but inherit a subtle failure mode: trained on clean data, they cannot recognise when an observation is unreliable, and treat degraded regions such as glass, mirrors, and overexposed surfaces as valid evidence for planning. We present a reliability-aware diffusion planner for 3D UAV navigation. It conditions trajectory generation on the observation together with a scene-level reliability heatmap that marks where perception cannot be trusted, produced by a lightweight network that distils the open-vocabulary reasoning of a vision-language model within the real-time planning budget. To generalise to unseen environments without retraining, we steer the denoising process with a differentiable two-stage ESDF cost that treats physical obstacles from depth and virtual obstacles from highly unreliable regions on equal footing. In simulation and on a real quadrotor, our planner produces markedly safer trajectories than a state-of-the-art diffusion baseline, reducing the obstacle-violation rate from 40.3% to 9.6% and raising the mean reliability of traversed regions from 0.588 to 0.925. Ablating the reliability term alone drops mean reliability from 0.898 to 0.783, confirming it as the decisive component, while distillation runs the framework up to 2 times faster than the full vision-language model.

---

## 论文详细总结（自动生成）

# 论文中文总结：Guided Diffusion with Distilled Vision-Language Reliability for Aerial Navigation

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：传统自主无人机导航采用“感知→建图→规划→控制”分阶段流水线，导致误差累积、延迟增加、需针对环境调参。端到端生成模型（如扩散模型）虽能直接映射原始观测到轨迹，但存在**关键缺陷**：在干净数据上训练后，无法识别玻璃、镜面、过曝等不可靠观测区域，将其错误视为有效规划依据。
- **意义**：本文提出**可靠性感知扩散规划器**，首次将感知可靠性作为轨迹生成的一等输入，提升室内无人机在退化环境中的导航安全性。

## 2. 方法论：核心思想、关键技术细节与算法流程

### 核心思想
- 利用视觉语言模型（VLM）的开放词汇推理能力估计场景级可靠性热力图，并蒸馏为轻量网络供实时使用；通过**可微的两阶段ESDF成本**，在扩散引导中同时处理深度物理障碍和不可靠区域虚拟障碍。

### 关键技术
- **可靠性估计**：
  - 教师模型：AnyTraverse（VLM），输入RGB图像，输出可靠性热力图 \(M \in [0,1]^{H \times W}\)（1-安全，0-不可靠）。定义失效类别（玻璃、反射、过曝等）权重均为1.0。
  - 蒸馏学生：轻量级MiT-B0 + 深度融合，训练目标包括特征模仿（余弦距离）、像素级加权L1（硬负样本权重）、SSIM结构损失，总损失 \(L_{\text{distill}} = w_{\text{feat}} L_{\text{feat}} + w_{\text{task}}[(1-w_{\text{ssim}})L_{\text{task}} + w_{\text{ssim}} L_{\text{SSIM}}]\)。
- **扩散轨迹规划器**：
  - 采用DDPM框架，1D U-Net \(\varepsilon_\theta\)预测噪声，条件 \(c = \{v_0, g, F_I, F_S\}\)（初始速度、目标、图像特征、可靠性图块）。
  - 状态表示：对比**Ego-path（绝对坐标）** vs **Delta-path（位移）**，最终选择Ego-path。
  - 辅助损失：平滑性、原点锚定、速度一致性、可靠性安全损失（投影到低可靠性区域的惩罚）。
- **无训练引导**：
  - 引导梯度：\(\tilde{\varepsilon}_\theta = \varepsilon_\theta - s \nabla_{x_t} C\)，总成本 \(C = w_{\text{obs}} C_{\text{obs}} + w_{\text{goal}} C_{\text{goal}} + w_{\text{perc}} C_{\text{perc}}\)。
  - 两阶段ESDF：阈值 \(\tau_h=0.3, \tau_s=0.6\)。可靠性 \(\leq \tau_h\) 的像素反向投影为**硬障碍**（加入ESDF）；介于 \(\tau_h, \tau_s\) 的作为**弱CHOMP成本**（激活距离更大）。物理障碍（深度点云）同样用ESDF计算 \(C_{\text{obs}}\)。
  - 引导步骤：通过逆扩散预测干净轨迹 \(\hat{x}_0\)，在度量空间计算梯度后更新噪声估计，再执行反向去噪。

## 3. 实验设计

- **数据集**：
  - 合成数据：Pegasus Simulator生成的仓库场景，配对RGB、深度、可靠性热力图、目标轨迹。
  - 真实数据：自定义8寸四旋翼，Intel RealSense D455相机，室内真实场景。
- **基准对比**：**NoMaD**（视觉条件扩散基线，不具备可靠性感知）。
- **消融实验**：
  - 状态表示：Ego-path vs Delta-path（有无引导）。
  - 组件消融：有无可靠性条件、有无ESDF引导的四组合。
- **评估指标**：路径效率、最小障碍距离、障碍违例率、穿越区域平均可靠性。

## 4. 资源与算力

- **硬件**：单张NVIDIA RTX 4070 GPU（显存占用500 MB），离线推理。
- **蒸馏加速**：学生网络使完整框架运行速度比VLM快2倍。
- **未明确**：训练时长、教师模型参数规模、训练数据集大小。

## 5. 实验数量与充分性

- **实验数量**：
  - 主对比：1个基线（NoMaD） + 2种表示（Ego/Delta）× 3种配置（完整、无可靠性、无引导） = 共7组（注意表2、表3中展示部分组合）。
  - 消融覆盖了可靠性条件和引导的开关，共计4种配置（表3）。
  - 仅一个合成环境 + 单一真实飞行测试。
- **充分性评价**：
  - **优点**：消融设计合理，逐项验证各组件贡献；报告标准差；与NoMaD对比明显。
  - **不足**：仅与一个基线对比（NoMaD），缺乏与更多扩散导航方法（如DTG、NaviDiffusor等）的比较；未在多种室内场景（办公室、走廊等）或户外环境中测试；真实实验仅一次飞行，未提供统计重复性。

## 6. 主要结论与发现

- **可靠性感知是决定性贡献**：
  - vs NoMaD：最小障碍距离 0.009m→0.562m，违例率 40.3%→9.6%，平均可靠性 0.588→0.925。
  - 消融可靠性引导：平均可靠性从0.898降至0.783，差距显著。
- **Ego-path表示优于Delta-path**：路径效率更高（0.590 vs 0.288），末端目标距离更近（0.710m vs 1.959m）。
- **引导使轨迹更安全但路径更长**：物理障碍和低可靠性区域迫使绕行，因此路径效率下降。
- 蒸馏使推理加速2倍，满足实时规划预算。

## 7. 优点

- **方法亮点**：
  - 首次将感知可靠性作为扩散规划器的显式条件，并融入引导，解决玻璃/反光等退化场景问题。
  - 蒸馏VLM为轻量网络，实现实时推理，保留语义先验。
  - 无训练泛化：仅需在线构建ESDF即可适应新环境，无需重训练。
  - 可微两阶段ESDF统一处理物理和虚拟障碍，设计合理。
- **实验亮点**：
  - 仿真+真实无人机验证，覆盖室内场景。
  - 多组消融实验清晰分离各组件贡献（条件vs引导）。
  - 使用标准差和多个安全指标，结果可信。

## 8. 不足与局限

- **可靠性估计局限**：热力图受限于教师模型（AnyTraverse）能力及蒸馏残差，为场景级而非像素校准，精度有限。
- **计算效率**：每步引导需查询ESDF，成本随去噪步数线性增长，难以在嵌入式平台高频率重规划。
- **实验覆盖**：
  - 仅与一个基线对比，缺乏与主流扩散导航方法的广泛比较。
  - 真实实验仅一次单机飞行，未展示统计显著性或多环境测试。
  - 仅在室内仓库环境验证，未涵盖室外、动态、光照骤变等复杂情况。
- **泛化性**：未验证跨场景迁移（如从仓库到办公室）的引导有效性；蒸馏学生可能对未见失效模式泛化不足。

（完）
