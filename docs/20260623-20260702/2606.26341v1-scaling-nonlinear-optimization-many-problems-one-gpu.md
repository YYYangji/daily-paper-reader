---
title: "Scaling Nonlinear Optimization: Many Problems One GPU"
title_zh: 缩放非线性优化：多问题单GPU
authors: "John Viljoen, Johanna Haffner, Masayoshi Tomizuka, Negar Mehr"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.26341v1"
tags: ["query:rpp"]
score: 8.0
evidence: GPU加速的非线性规划用于机器人轨迹优化
tldr: 机器人学中的非线性规划问题通常由CPU求解器逐个求解，无法利用GPU批处理加速。本文提出jaxipm，首个基于IPOPT的GPU批处理NLP求解器，通过异构迭代融合和迭代级批处理实现高效并发求解。在多旋翼模型预测控制任务中，吞吐量相比IPOPT提升高达32.85倍。该工作使NLP求解器能集成到现代GPU批处理框架中。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26341v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1653, \"height\": 751}, {\"url\": \"assets/figures/arxiv/2606.26341v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 904, \"height\": 526}, {\"url\": \"assets/figures/arxiv/2606.26341v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 819, \"height\": 749}, {\"url\": \"assets/figures/arxiv/2606.26341v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 602, \"height\": 557}, {\"url\": \"assets/figures/arxiv/2606.26341v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 890, \"height\": 463}, {\"url\": \"assets/figures/arxiv/2606.26341v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 875, \"height\": 544}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.26341v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 921, \"height\": 1064}, {\"url\": \"assets/tables/arxiv/2606.26341v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 823, \"height\": 312}, {\"url\": \"assets/tables/arxiv/2606.26341v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 766, \"height\": 427}, {\"url\": \"assets/tables/arxiv/2606.26341v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 753, \"height\": 310}]"
motivation: 现有NLP求解器为CPU单问题求解，无法融入GPU批处理学习管道，限制了吞吐量。
method: 通过异构迭代融合消除控制流，采用迭代级批处理最小化GPU空闲时间，在JAX中实现。
result: 在多旋翼NMPC基准上，吞吐量相比IPOPT提升高达32.85倍。
conclusion: 首个GPU批处理NLP求解器，实现了NLP求解器与GPU批处理框架的无缝集成。
---

## 摘要
许多机器人问题，包括轨迹优化、逆运动学和接触丰富的运动规划，都可以归结为非线性规划问题。成熟的NLP求解器如IPOPT可以解决这些问题，提供硬约束满足、最优性保证以及随问题维度扩展的良好性能。这些求解器支撑着机器人学中的梯度方法，但它们仍然受限于CPU，并且一次只能求解一个问题，阻碍了它们集成到GPU批处理学习流程中。另一方面，基于采样的方法，如强化学习、模型预测路径积分和模仿学习，已成为现代机器人学研究的核心，因为它们能够利用GPU批处理模拟器。这些模拟器每秒可以产生比以往多几个数量级的动态推演。如果存在一个GPU批处理NLP求解器，它将在每秒生成的受约束局部最优解数量上解锁类似的加速。这种一次求解多个问题与一次求解单个问题的机制，是将NLP求解器集成到现代GPU批处理机器人框架中的关键要求。为此，我们介绍了\texttt{jaxipm}，这是第一个基于IPOPT并在JAX中实现的GPU批处理NLP求解器。我们通过重新设计IPOPT的算法，利用\textit{异构迭代融合}消除控制流，并通过\textit{迭代级批处理}最小化GPU空闲时间来实现这一目标。我们在各种四旋翼非线性模型预测控制基准上评估了\texttt{jaxipm}，包括存在障碍物时的参考跟踪、多四旋翼无碰撞导航以及杂乱环境中的导航。我们展示了相比IPOPT高达$32.85\times$的吞吐量提升。我们的完整开源代码库可在https://github.com/johnviljoen/jaxipm获取。

## Abstract
Many robotics problems, including trajectory optimization, inverse kinematics, and contact-rich motion planning, reduce to nonlinear programs (NLPs). Mature NLP solvers such as IPOPT can solve these problems, offering hard constraint satisfaction, optimality guarantees, and favorable scaling with problem dimension. These solvers underpin gradient-based methods in robotics, yet remain CPU-bound and solve only one problem at a time, preventing their integration into GPU-batched learning pipelines. On the other hand, sampling-based approaches such as reinforcement learning, model predictive path integral, and imitation learning have become the core of modern robotics research due to their ability to leverage GPU-batched simulators. These simulators can generate orders of magnitude more dynamics rollouts per second than was previously possible. If a GPU-batched NLP solver existed, it would unlock similar speedups in the number of constrained, locally optimal solutions generated per second. This regime of solving many problems concurrently versus solving a single problem at a time is a key requirement for integrating NLP solvers in modern GPU-batched robotics frameworks. To this end, we introduce \texttt{jaxipm}, the first GPU-batched NLP solver, based on IPOPT, and implemented in JAX. We accomplish this by redesigning IPOPT's algorithm to eliminate control flow with \textit{heterogeneous iteration fusion}, and by minimizing GPU idle time with \textit{iteration level batching}. We evaluate \texttt{jaxipm} on a variety of quadrotor nonlinear model predictive control benchmarks, including reference tracking in the presence of obstacles, multi-quadrotor navigation without collision, and navigation in a cluttered environment. We demonstrate up to a $32.85\times$ increase in throughput over IPOPT. Our complete open-source codebase is available at https://github.com/johnviljoen/jaxipm.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **问题**：机器人学中的轨迹优化、逆运动学、接触丰富的运动规划等许多问题均归结为非线性规划（NLP）。成熟的 NLP 求解器（如 IPOPT）虽然可以提供硬约束满足、最优性保证和良好的维度扩展，但它们都是 **CPU 单问题求解**，无法融入现代 GPU 批处理学习流程（如强化学习、MPPI、模仿学习），这成为整个管线的瓶颈。
- **背景**：采样方法得益于 GPU 批处理模拟器，能够每秒生成大量动态推演。如果存在一个 **GPU 批处理的 NLP 求解器**，则能够在受约束、局部最优解的吞吐量上获得类似数量级的加速。
- **目标**：提出 **jaxipm**，首个基于 IPOPT 并完全在 JAX 中实现的 **GPU 批处理 NLP 求解器**，实现一次求解多个而非单个问题的能力。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：通过重新设计 IPOPT 的算法，消除控制流分支带来的 GPU 异构执行问题，并减少 GPU 空闲时间。
- **关键技术细节**：
  - **异构迭代融合（Heterogeneous Iteration Fusion）**：
    - IPOPT 有多种回退机制（回溯线搜索、二阶校正、可行性恢复、监视器等），不同问题在同一迭代可能走不同分支。
    - 将每个分支的计算分解为 **共享计算**（如线性系统求解、代价函数求值）和 **分支特有计算**。共享计算一次性完成所有问题；对于分支特有计算，为所有问题计算所有分支，然后根据各自选择的结果进行筛选，从而让所有问题执行完全一致的迭代体。
  - **迭代级批处理（Iteration Level Batching）**：
    - 不像传统批处理（等待整个批结束才启动下一批），当某个优化问题收敛后，立即初始化一个新问题替换它，未收敛的问题继续迭代。
    - 在迭代边界上进行同步，最大化 GPU 利用率，避免因“最慢成员”导致的空闲时间。
- **实现细节**：
  - 基于 **JAX** 进行编写，自然融合自动微分、GPU 加速。
  - 线性求解器使用 **cuDSS**（NVIDIA 的 GPU 直接稀疏求解器）。
  - KKT 系统的组装、惯性修正等完全在 GPU 上执行，算法逻辑也部署在 GPU 上（而非 CPU 控制）。
- **算法流程**（文字说明）：
  1. 初始化一批 NLP 问题（参数不同，结构相同）。
  2. 每步迭代中，对所有问题同时执行统一的“融合迭代”：
     - a) 计算所有问题的共享量（如梯度、Jacobian、Hessian、KLT 系统组装）。
     - b) 对所有问题计算所有可能分支所需的特有量（例如额外的线性求解用于二阶校正、向量操作用于线搜索等）。
     - c) 根据每个问题实际选择的分支，从计算结果中筛选正确的更新量。
  3. 更新状态、检查收敛性。对于已收敛的问题，立即热启动新问题（继承上一轮的参数和状态以减少初始化开销）。
  4. 重复迭代直至所有问题收敛或达到限制。

## 3. 实验设计：数据集 / 场景、基准、对比方法
- **场景**：全部基于 **四旋翼非线性模型预测控制（NMPC）**，采用 13 自由度模型，时间步长 0.1s，预测时域 30，每个优化至少 506 个变量。
  - **单四旋翼导航**（障碍物场景）：初始位置分布在 90° 或 180° 扇形内，飞向共同目标并避开障碍。
  - **参考跟踪 + 时变障碍**：四旋翼跟踪预设参考轨迹，同时避开动态障碍；通过改变参考速度 $\bar{v}$（1.0, 1.5, 2.0 m/s）调整难度。
  - **多四旋翼共识（multi-quadrotor consensus）**：2 或 4 架四旋翼交换位置，同时保持相互间最小间隔；变量数从 1012 增长到 2024。
- **基准方法**：
  - **CasADi/IPOPT**：经典的 CPU 实现（单问题求解）。
  - **MadNLP**（ExaModels/MadNLP）：GPU 加速了线性求解和导数计算，但控制逻辑仍在 CPU 上，无法批处理。
- **评估指标**：吞吐量（solves/s）、相对加速比、平均最终代价、平均迭代次数、正确性（步骤级偏差 ECDF）。

## 4. 资源与算力
- **文中未明确说明**使用的具体 GPU 型号、数量、训练时长或运行时间设置。
  - 仅提到使用了 NVIDIA 的 **cuDSS** 作为稀疏线性求解器，表明在一张 GPU 上运行。
  - 未披露批大小、系统内存、实验重复次数等细节。
- 因此，该部分信息不充分，无法量化实际计算开销。

## 5. 实验数量与充分性
- **实验组数**：
  - 吞吐量实验：3 个主要任务（扇形导航两个子场景、参考跟踪三个速度、多机两个规模），共 7 组直接吞吐量对比。
  - 正确性验证：1 个复杂优化问题，逐迭代与 IPOPT 对比，给出所有迭代的偏差 ECDF 图。
  - 图示展示了迭代计数分布、轨迹可视化等。
- **充分性与客观性**：
  - 覆盖了不同难度、不同维度、异构与同构迭代场景，验证了 **异构迭代融合** 和 **迭代级批处理** 的有效性。
  - 与 MadNLP 的对比显示了即使 GPU 加速线性求解，但未批处理时吞吐量仍远低于 jaxipm。
  - 正确性验证使用归一化偏差小于 $10^{-8}$，证明算法等价于 IPOPT。
  - 潜在不足：未与直接对多个 IPOPT 进程使用 GPU（如利用 JAX 手动批处理而保持原 IPOPT 控制流）的基线对比；未测试除四旋翼以外的 NLP 问题（如逆运动学、接触力学）；未报告统计重复次数和方差。

## 6. 论文的主要结论与发现
- **主要结论**：jaxipm 首次实现了 GPU 批处理 NLP 求解，在四旋翼 NMPC 基准上吞吐量比 IPOPT 提升 **最高 32.85×**，比 MadNLP 也有显著优势。
- 迭代级批处理比解决级批处理更高效（图 2 说明）。
- 异构迭代融合成功消除了分支带来的障碍，同时保持了与 IPOPT 相同的解质量（图 6 正确性验证）。
- 随着问题维度增大，由于 GPU 内存限制导致 batch size 减小，加速比有所下降（从 32.85× 降至 16.03× 当从 2 架四旋翼增至 4 架），但仍维持一个数量级以上的优势。

## 7. 优点：方法或实验设计上的亮点
- **方法论创新**：
  - 第一次提出 **异构迭代融合** 来处理 NLP 求解器中固有的控制流分支，使所有问题在 GPU 上同步前进。
  - **迭代级批处理** 具有实际意义，避免了“木桶效应”，显著提高 GPU 利用率。
  - 完全在 JAX 中实现，可直接融入强化学习框架（如 Brax、MJX）实现端到端学习。
- **实验设计**：
  - 测试了多种任务（导航、跟踪、共识）和难度（速度、维度），覆盖了同构/异构迭代场景。
  - 正确性对比直接使用 IPOPT 的中间状态，方法严谨。
  - 提供开源代码，有可复现性。

## 8. 不足与局限
- **实验覆盖**：
  - 仅使用四旋翼 NMPC 一种应用，虽然代表性较强，但未能展示在更多机器人问题（如接触丰富的操作、人体运动规划）上的泛化能力。
  - 未报告不同 batch size 对吞吐量的影响，仅给出基准数字。
- **硬件信息缺失**：未说明 GPU 型号、内存大小、批大小等关键配置，无法评估可移植性及与 MadNLP/ IPOPT 的公平比较（MadNLP 可能在不同 GPU 上运行）。
- **对比局限性**：
  - 未与潜在的“多进程 IPOPT + GPU 批处理求值”的混合基线对比。
  - 未讨论时间约束（如实时性）或数值稳定性。
- **规模限制**：论文指出随着维度增加，GPU 内存不足导致 batch size 减小，速度提升下降；未探索稀疏性或更高效的线性求解策略来缓解。
- **缺少消融研究**：未单独量化“异构迭代融合”与“迭代级批处理”各自的贡献大小。
- **应用场景**：当前解决的是**互相独立的 NLP 批**，未涉及问题间有耦合的场景（如多智能体协同优化），但论文声明聚焦于独立问题批处理。

（完）
